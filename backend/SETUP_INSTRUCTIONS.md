# Инструкции по настройке

## 1. Установка зависимостей

```bash
cd backend
source venv/bin/activate
pip install -r requirements.txt
```

## 2. Очистка пользователей (кроме admin)

Удалить всех пользователей кроме admin:
```bash
python manage.py clean_users
```

## 3. Настройка логирования

Для цветного логирования установлен пакет `colorlog`. Если его нет:
```bash
pip install colorlog
```

## 4. Запуск сервера

```bash
python manage.py runserver
```

## Цвета в логах

- ✅ **Зеленый (INFO)** - успешные операции (вход, регистрация)
- ⚠️ **Желтый (WARNING)** - предупреждения
- ❌ **Красный (ERROR)** - ошибки
- 🔵 **Синий** - информация и время

## Доступ к системе

- **Admin**: username=`admin`, password=`admin123`
- Для создания нового admin с другим паролем:
  ```bash
  python manage.py create_admin
  ```


